import * as _chai from 'chai';
import * as money from '../../src/model/money';

// describe('GBP', () => {
//     it('can use GBP when GBP expected', () => {
//         let gbp = BigInt(10) as money.GBP
//         _chai.expect(() => (m : money.GBP) => true), gbp).to.throw();
//     })
// })